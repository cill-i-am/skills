interface:
  display_name: "Grilling"
  short_description: "Stress-test plans before building."
  default_prompt: "Use $grilling to challenge a product, architecture, PRD, or implementation plan against existing code and docs before committing to a build path."
