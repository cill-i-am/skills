interface:
  display_name: "Wayfinder"
  short_description: "Resolve decisions before a PRD is ready."
  default_prompt: "Use $wayfinder to map and resolve the decisions needed for a trustworthy PRD in this project's chosen source."
