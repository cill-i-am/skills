interface:
  display_name: "Repository Instructions"
  short_description: "Keep repository guidance small and clear"
  default_prompt: "Use $build-intent-layer to remove stale guidance and place durable constraints at the scope that owns them."
