interface:
  display_name: "TDD"
  short_description: "Develop behaviour through red-green-refactor"
  default_prompt: "Use $tdd to implement the requested behaviour through meaningful failing tests, a working change, and refactoring."
