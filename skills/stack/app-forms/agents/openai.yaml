interface:
  display_name: "App Forms"
  short_description: "Build accessible TanStack Form flows"
  default_prompt: "Use $app-forms for the requested form using existing fields, validation, and mutation contracts."
