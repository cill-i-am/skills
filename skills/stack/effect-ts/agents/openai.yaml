interface:
  display_name: "Effect TS"
  short_description: "Source-backed Effect guidance."
  default_prompt: "Use $effect-ts to implement or review Effect services, Layers, Schema boundaries, typed errors, retries, observability, HttpApi/RPC clients, SQL, scoped resources, concurrency, or tests."
