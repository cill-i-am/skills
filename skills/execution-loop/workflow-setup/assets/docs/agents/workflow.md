# Project Workflow

- Planning and delivery source: <linear or repo; choose one>
- Canonical plans / PRDs: <actual location>
- Canonical work items: <actual location>
- Shared record branch: <repo mode only; omit for Linear>
- Execution policy: <existing policy path or link>
- Readiness / state conventions: <existing convention or triage-states.md>

This declaration owns the project's source choice and locations. Other instructions link here. Skills use the matching reference in `workflow-setup` for storage-specific operations.

Plans own product intent; work items own scope, dependencies, status, owner, and concise delivery evidence. Code, domain language, ADRs, PRs, and checks keep their own canonical homes. Link to those sources rather than duplicating them.

Use observable outcome titles. Group work under a plan or parent outcome when useful, keeping grouping separate from dependencies. Dispatch only ready delivery work with clear acceptance criteria, scope, dependencies, proof expectations, and genuine external gates.

A draft or local branch proposal is not published shared state. Completion requires evidence for the claimed outcome and the authorized acceptance decision. Refresh current records and linked PR/check evidence before assigning work or changing delivery state.

Follow the user's scope, applicable project instructions, and existing authority. The source choice grants no permission to send messages, create tasks, merge, deploy, or migrate records.
