interface:
  display_name: "Worker"
  short_description: "Deliver one ready work item with proof."
  default_prompt: "Use $worker to implement the assigned ready work item, verify its outcome, and prepare the requested PR."
