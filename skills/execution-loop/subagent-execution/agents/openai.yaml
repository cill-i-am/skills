interface:
  display_name: "Subagent Execution"
  short_description: "Delegate bounded support probes."
  default_prompt: "Use $subagent-execution to delegate bounded investigations, reviews, runtime smoke checks, or subtasks while keeping the worker or orchestrator in control."
