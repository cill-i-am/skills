interface:
  display_name: "Simplify"
  short_description: "Remove complexity from the changed code"
  default_prompt: "Use $simplify to remove unnecessary complexity while preserving the requested behaviour and contracts."
