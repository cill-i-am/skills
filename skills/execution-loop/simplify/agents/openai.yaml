interface:
  display_name: "Simplify"
  short_description: "Remove unnecessary implementation complexity."
  default_prompt: "Use $simplify before wrapping up changed code or a PR to remove unnecessary abstraction, fallback logic, duplication, broad scope, and avoidable complexity."
