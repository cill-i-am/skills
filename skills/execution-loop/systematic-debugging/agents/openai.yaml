interface:
  display_name: "Systematic Debugging"
  short_description: "Find root causes with a red loop."
  default_prompt: "Use $systematic-debugging to reproduce a bug, prove a red feedback loop, identify root cause, fix it, and verify the loop is green."
