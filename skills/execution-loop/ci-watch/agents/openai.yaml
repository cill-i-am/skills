interface:
  display_name: "CI Watch"
  short_description: "Own one PR's next CI poll."
  default_prompt: "Use $ci-watch to own one PR's next poll, report current-head deltas, fix bounded in-scope failures, and stop when green, closed, merged, or genuinely blocked."
