interface:
  display_name: "Triage"
  short_description: "Prepare work items for the next step."
  default_prompt: "Use $triage to clarify, classify, unblock, or close work using the project's current records."
